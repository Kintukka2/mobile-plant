{
  "name": "sprout-native",
  "version": "0.1.0",
  "private": true,
  "description": "The store wrapper around the Sprout web app. The app itself has no build step; this does.",
  "scripts": {
    "copy": "node copy-web.js",
    "sync": "npm run copy && cap sync",
    "ios": "npm run sync && cap open ios",
    "android": "npm run sync && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
